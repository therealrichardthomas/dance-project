const express = require('express'); // using express modules
const path = require('path'); 
const fs = require('fs'); // using a file system
const app = express(); // importing express
const port = 80; // creating a port to run my files

